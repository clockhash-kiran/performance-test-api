{
  "name": "performance-test-api",
  "version": "1.0.0",
  "description": "API for performance testing with JMeter and K6",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "morgan": "^1.10.0"
  }
}
